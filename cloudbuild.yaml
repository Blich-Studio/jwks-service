steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    secretEnv: ['NPM_TOKEN']
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        docker build \
          --build-arg NPM_TOKEN="$$NPM_TOKEN" \
          -t gcr.io/$PROJECT_ID/jwks-service:$BUILD_ID \
          -t gcr.io/$PROJECT_ID/jwks-service:latest \
          -f Dockerfile \
          .

  # Push the container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/jwks-service:$BUILD_ID'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/jwks-service:latest'

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'jwks-service'
      - '--image'
      - 'gcr.io/$PROJECT_ID/jwks-service:latest'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'JWT_ISSUER=${_JWT_ISSUER},JWT_AUDIENCE=${_JWT_AUDIENCE},JWT_KID=${_JWT_KID},JWT_EXPIRES_IN=15m,JWKS_CACHE_MAX_AGE=300'
      - '--set-secrets'
      - 'GOOGLE_PRIVATE_KEY_SECRET=auth-issuer-private-key-rsa:latest,TOKEN_API_KEY=jwks-token-api-key:latest'
      - '--max-instances'
      - '10'
      - '--min-instances'
      - '1'
      - '--port'
      - '3100'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--timeout'
      - '30'

images:
  - 'gcr.io/$PROJECT_ID/jwks-service:$BUILD_ID'
  - 'gcr.io/$PROJECT_ID/jwks-service:latest'

substitutions:
  _REGION: europe-west1
  _JWT_ISSUER: https://jwks.blichstudio.com
  _JWT_AUDIENCE: blich-api
  _JWT_KID: prod-key-1

availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/NPM_TOKEN_CLOUDBUILD/versions/latest
      env: 'NPM_TOKEN'

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'
